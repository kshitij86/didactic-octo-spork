//https://codingcompetitions.withgoogle.com/kickstart/round/000000000019ffc7/00000000001d40bb

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T, n, k, p;

    cin >> T;
    while (T--)
    {
        cin >> n >> k >> p;
        int stacks[n][k];

        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < k j++)
            {
                cin >> stacks[i][j];
            }
        }
    }
}